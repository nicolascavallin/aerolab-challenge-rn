import React, { FC } from "react";
import { Text, TouchableOpacity, View } from "react-native";

interface AboutScreenProps {}

const AboutScreen: FC<AboutScreenProps> = (props) => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default AboutScreen;
